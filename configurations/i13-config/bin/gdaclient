#runs the LogPanel and RCP client

#identify install folder and JAVA_HOME
source /dls_sw/i13/etc/i13_profile.sh


#gdalog

export JAVA_OPTS="$JAVA_OPTS -Dgda.mode=$GDAMODE"

if [ "$1" = "--clean" ]
then
    echo "rf /scratch/i13i/.workspace-$(whoami)"
    rm -rf /scratch/i13i/.workspace-$(whoami)
fi

echo "$GDAFOLDER/client/gda-i13i"

$GDAFOLDER/client/gda-i13i

