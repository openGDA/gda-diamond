var compatibleProperties = "sds.boolean";
var description = "strToBool(window name)";
var parameters = new Array("str here");

function execute(args) {

	var argument1 = args[0];
	
	Packages.java.lang.System.out.println("Display = "+argument1+"");
	
	if ( argument1 == 'yes')
	    return true;
	    
	if ( argument1 == 'no')
	    return false;
}