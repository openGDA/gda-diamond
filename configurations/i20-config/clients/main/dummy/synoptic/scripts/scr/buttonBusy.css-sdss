importPackage(Packages.org.csstudio.sds.ui.scripting);

var returns = "java.lang.Boolean";
var description = "button status";
var parameters = new Array("Description of argument 1");
var parameterTypes = new Array("java.lang.Double");

function execute(args) {

	var argument1 = args[0];
	
	if ( argument1 == 'Ready')
	    return true;
	    
	if ( argument1 == 'Busy')
	    return false;
}