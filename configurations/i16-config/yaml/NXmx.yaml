entry/:
  NX_class@: NXentry # the '@' has to come at the end as 
  #definition: NXmx
  #end_time_estimated: /entry/end_time

  # Fix ValidationReports -----------------------------------------------------

  # Needed to get rid of
  # ?
  #data: /entry/measurement

  # -------------------- ValidationReport - NXmx: Data node 'name': must be set
  # try:
  #name: /entry/instrument/name

  # -------------- ValidationReport - NXmx: Attribute 'short_name': must be set
  # Tried
  #short_name@: /entry/instrument/name
  # but this failed with
  # Applying template 'NXmx.yaml' failed:Link path must be to attribute (i.e. it must end with '@'),
  # 	for attribute with name short_name and link path Optional[/entry/instrument/name]

  # ------ ValidationReport - NXmx: Group node 'entry/sample/transformations/kphi':
  # 			No such NXtransformation group: entry/sample/transformations/kphi

  # ------------------------------- ValidationReport - NXmx: Group node 'unnamed':
  #							 At least one group of type NXsource must be present

  instrument/:
    NX_class@: NXinstrument
    transformations/:
      NX_class@: NXtransformations
      offsetdelta: /entry/instrument/delta_axis_offset/value
      delta: /entry/instrument/kdelta/value
      gamma: /entry/instrument/kgam/value

  sample/:
    NX_class@: NXsample
    transformations/:
      NX_class@: NXtransformations
      cryophi: /entry/instrument/cryophi/value
      phi: /entry/instrument/kphi/value
      kappa: /entry/instrument/kap/value
      theta: /entry/instrument/kth/value
      mu: /entry/instrument/kmu/value

