These file were downloaded and stored here as they are closely related to nexus templates used to create subentry that conforms to each application definition.

NXDL schemas are downloaded and stored in parent folder as they are used to resolve parsing error reported by eclipse editor.