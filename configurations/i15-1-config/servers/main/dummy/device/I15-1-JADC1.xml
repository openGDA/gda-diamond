<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://www.springframework.org/schema/beans
						http://www.springframework.org/schema/beans/spring-beans.xsd">

	<description>	Unlike other dummy detectors which requires an external Area Detector IOC to be running
					this simulator is run within GDA, so there are no external dependencies.
	</description>

	<bean id="adc1_adbase"
			class="gda.device.detector.areadetector.v17.impl.ADBaseSimulator">
	</bean>
</beans>