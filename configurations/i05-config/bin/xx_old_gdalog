#! /bin/sh

export LESS=
COMMAND="less -f +F /dls_sw/i05/logs/gda_server.log"

case $TERM in
	xterm)
		exec gnome-terminal --title="GDA Server Logfile" -x $COMMAND
		;;
	*) 
		exec $COMMAND
		;;
esac
