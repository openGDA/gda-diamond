#!/bin/bash
echo "delete gda_commands"
here=`dirname $0`
echo $here
config=`cd $here/../../; pwd`
echo $config
if [  -d "$config/../../plugins" ]; then
	root=$config/../../plugins
else
	root=$config/../plugins
fi
echo $root

python2.6  ${root}/uk.ac.gda.core/bin/gda  --config=${config} $@
